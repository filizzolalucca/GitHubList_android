package com.example.githublist.features.pullRequestRepo.data.schecmas

data class PullSchemas(val title: String,
                       val user: User,
                       val body: String
                       )
